# Removed Python server files

The Flask-based server and Python scaffolding were removed in favor of a static client-side wiki that reads data from data/content.json.

If you relied on the Python files earlier, consider restoring from the feature/wiki-organization commit history or the previous commits.
